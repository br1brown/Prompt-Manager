services:
  prompt-manager:
    build: .
    ports:
      - "8006:80"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.prompt-manager.rule=Host(`prompt-manager.localhost`)"
      - "traefik.http.services.prompt-manager.loadbalancer.server.port=80"
    restart: unless-stopped
